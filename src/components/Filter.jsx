// client/src/components/Filter.jsx
/** Controls for viewing, searching, and sorting todos. */
export default function Filter({ filter, setFilter, search, setSearch, sort, setSort }) {
  return <section className="card space-y-3 p-3 sm:flex sm:items-center sm:justify-between sm:space-y-0"><div className="flex rounded-lg bg-slate-100 p-1 dark:bg-slate-800" role="group" aria-label="Filter tasks">{['all', 'active', 'completed'].map((value) => <button key={value} onClick={() => setFilter(value)} className={`flex-1 rounded-md px-3 py-1.5 text-sm capitalize ${filter === value ? 'bg-white font-semibold shadow dark:bg-slate-700' : 'text-slate-500'}`}>{value}</button>)}</div><div className="flex gap-2"><input aria-label="Search todos" value={search} onChange={(e) => setSearch(e.target.value)} className="field min-w-0" placeholder="Search tasks…" /><select aria-label="Sort todos" value={sort} onChange={(e) => setSort(e.target.value)} className="field w-28"><option value="date">Newest</option><option value="priority">Priority</option></select></div></section>;
}
